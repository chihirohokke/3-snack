<div class="center jumbotron jumbotron-extend">
  <div class="text-center container-fluid jumbotron-container">
    <h1 class="app-title">3:snack<i class="fas fa-cookie-bite"></i></h1>
    <h5>3時のおやつをシェアしませんか？</h5>
    <% if logged_in? %>
      <p>ログイン中: <%= current_user.name %></p>
    <% else %>
      <div class="mt-3">
      <%= link_to 'Join!', join_path, class: 'btn btn-lg btn-black' %>
      <%= link_to 'Login', login_path, class: 'btn btn-lg btn-black' %>
      </div>
    <% end %>  
    <p class="pt-3">手作りsweetsの写真を投稿して、レシピなどを共有できます<br>気になった写真にはいいね【sweetボタン】でお気に入りにいれましょう</p>
  </div>
</div>
